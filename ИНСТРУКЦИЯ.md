# 📖 Полная инструкция по проекту "Valentines"

## 🎯 Описание проекта

Это интерактивное веб-приложение на Next.js для признания в любви на День Святого Валентина. Приложение состоит из двух основных частей:
1. **Игра "Найди пару"** - карточная игра на память с фотографиями в форме сердца
2. **Предложение стать валентинкой** - романтичное предложение с фейерверками

---

## 📂 Структура проекта

```
valentines/
├── public/
│   ├── game-photos/          # Фотографии для игры
│   │   ├── 1.HEIC - 10.HEIC  # 10 фото в формате HEIC
│   │   └── 11.avif - 36.avif # 26 фото в формате AVIF
│   └── last/                 # Дополнительные фото (не используются в игре)
├── src/
│   ├── app/
│   │   ├── page.tsx          # Главная страница (основной компонент)
│   │   ├── layout.tsx        # Общий layout приложения
│   │   └── globals.css       # Глобальные стили
│   └── components/
│       ├── PhotoPairGame.tsx      # Компонент игры "Найди пару"
│       ├── ValentinesProposal.tsx # Компонент предложения
│       ├── TextFooter.tsx         # Текстовые подсказки внизу экрана
│       └── OrientationGuard.tsx   # Защита от портретной ориентации
├── package.json              # Зависимости проекта
└── next.config.ts           # Конфигурация Next.js
```

---

## 🎮 Как работает приложение (по порядку)

### 1️⃣ **Запуск приложения** ([page.tsx](src/app/page.tsx))

При запуске приложения:
- Проверяется ориентация экрана (через `OrientationGuard`)
- На мобильных устройствах: **ОБЯЗАТЕЛЬНА** горизонтальная ориентация
- На десктопе: работает в любой ориентации
- Показывается игра `PhotoPairGame` с текстовыми подсказками `TextFooter`

### 2️⃣ **Защита ориентации** ([OrientationGuard.tsx](src/components/OrientationGuard.tsx))

**Условие блокировки:**
```javascript
window.innerHeight > window.innerWidth && window.innerWidth < 1024
```

- Если экран меньше 1024px И находится в портретном режиме → показывается экран с просьбой повернуть устройство
- Анимированная иконка телефона с инструкцией "Rotate your device"
- Игра становится доступной только в ландшафтном режиме

### 3️⃣ **Игра "Найди пару"** ([PhotoPairGame.tsx](src/components/PhotoPairGame.tsx))

#### 📊 Механика игры:

**Сколько нужно фотографий: 18 УНИКАЛЬНЫХ ФОТО**

**Почему 36 карточек?**
- ✅ **18 уникальных фотографий** × 2 = **36 карточек** (каждая фото встречается дважды)
- Это классическая игра "Memory" - нужно найти пары одинаковых фото

#### 🖼️ Используемые фотографии:

```javascript
const images = [
  "/game-photos/1.HEIC",    // Файлы 1-10 в формате HEIC
  "/game-photos/2.HEIC",
  ...
  "/game-photos/10.HEIC",
  "/game-photos/11.avif",   // Файлы 11-18 в формате AVIF
  "/game-photos/12.avif",
  ...
  "/game-photos/18.avif",   // ВСЕГО: 18 уникальных фото
];
```

**⚠️ ВАЖНО:** 
- В игре используются только **фото с номерами 1-18** (первые 10 в HEIC + следующие 8 в AVIF)
- Фото 19-36 в папке `game-photos/` **НЕ используются в игре** - они нужны только для компонента `ValentinesProposal` (фоновая сетка)
- Папка `last/` вообще не используется в коде

#### ❤️ Расположение карточек:

Карточки расположены в форме **СЕРДЦА** (7 рядов × 9 колонок):
```javascript
const heartLayout = [
  [null, null, 0, 1, null, 2, 3, null, null],        // Ряд 1: 4 карточки
  [null, 4, 5, 6, 7, 8, 9, 10, null],                // Ряд 2: 7 карточек
  [11, 12, 13, 14, 15, 16, 17, 18, 19],              // Ряд 3: 9 карточек (самый широкий)
  [null, 20, 21, 22, 23, 24, 25, 26, null],          // Ряд 4: 7 карточек
  [null, null, 27, 28, 29, 30, 31, null, null],      // Ряд 5: 5 карточек
  [null, null, null, 32, 33, 34, null, null, null],  // Ряд 6: 3 карточки
  [null, null, null, null, 35, null, null, null, null], // Ряд 7: 1 карточка
];
// null = пустое место, цифры = индексы карточек (0-35)
```

#### 🎯 Правила игры:

1. **Начало:** Все 36 карточек (18 пар) перемешиваются и размещаются рубашкой вверх
2. **Клик:** Игрок кликает на карточку → она переворачивается с анимацией (0.5 сек)
3. **Выбор второй карточки:**
   - Если совпадают → обе остаются открытыми ✅
   - Если НЕ совпадают → показывается красная вспышка, через 1 секунду карточки переворачиваются обратно ❌
4. **Победа:** Когда все 36 карточек открыты → автоматический переход к предложению

#### 🎨 Анимации:

- **Переворот карточки:** 3D-эффект `rotateY` (0.5 сек)
- **Наведение:** Увеличение `scale: 1.1`
- **Ошибка:** Красная вспышка + пульсация

#### 📱 Адаптивность:

- **Мобильные:** `11vh × 11vh` (размер зависит от высоты экрана)
- **Десктоп (>1024px):** `20px × 20px`
- Промежутки: `gap-1` на мобильных, `gap-2` на десктопе

### 4️⃣ **Текстовые подсказки** ([TextFooter.tsx](src/components/TextFooter.tsx))

Во время игры внизу экрана показываются подсказки:
- **Слева:** "Соответствовать фото пары"
- **Справа:** "раскрыть сюрприз"

### 5️⃣ **Переход к предложению**

При победе в игре:
- Запускается `handleShowProposal()`
- Устанавливается флаг `isTransitioning = true`
- Игра плавно исчезает (2 секунды)
- Через 2 секунды показывается компонент `ValentinesProposal`

### 6️⃣ **Предложение стать валентинкой** ([ValentinesProposal.tsx](src/components/ValentinesProposal.tsx))

#### 📝 Этапы (шаги):

**Шаг 0 (5 секунд):**
```
"Congratulations! You have completed the game."
```

**Шаг 1 (5 секунд):**
```
"I have a surprise for you!"
```

**Шаг 2:**
```
Фоновая сетка с 36 фото (полупрозрачная)
Грустный хомяк (sad_hamster.png)
"Will you be my Valentine?"

Две кнопки:
- "Yes, I will! 🥰" - при клике → фейерверк + шаг 3
- "No, I won't 😢" - при наведении/клике → кнопка убегает в случайное место экрана
```

**Шаг 3:**
```
Фейерверк на весь экран
Прыгающий хомяк (hamster_jumping.gif)
"Thank you for accepting, I love you! 💕"
"For more information, write me!!! 💌"
```

#### 🎆 Особенности:

- **Убегающая кнопка "No":** Использует `getRandomPosition()` для случайного перемещения по экрану (80% × 80% области)
- **Фейерверк:** Библиотека `@fireworks-js/react` на весь экран
- **Фоновая сетка:** 36 фотографий (ВСЕ фото 1-36) в сетке 6×6 с прозрачностью 10%

---

## 🖼️ Требования к фотографиям

### ✅ Для игры (ОБЯЗАТЕЛЬНО):

**Нужно ровно 18 уникальных фотографий:**

```
public/game-photos/
├── 1.HEIC
├── 2.HEIC
├── 3.HEIC
├── 4.HEIC
├── 5.HEIC
├── 6.HEIC
├── 7.HEIC
├── 8.HEIC
├── 9.HEIC
├── 10.HEIC
├── 11.avif
├── 12.avif
├── 13.avif
├── 14.avif
├── 15.avif
├── 16.avif
├── 17.avif
└── 18.avif
```

### 📋 Для фонового коллажа:

**Дополнительно 18 фотографий для фоновой сетки на экране предложения:**

```
public/game-photos/
├── 19.avif
├── 20.avif
├── 21.avif
├── 22.avif
├── 23.avif
├── 24.avif
├── 25.avif
├── 26.avif
├── 27.avif
├── 28.avif
├── 29.avif
├── 30.avif
├── 31.avif
├── 32.avif
├── 33.avif
├── 34.avif
├── 35.avif
└── 36.avif
```

### 🎨 Дополнительные изображения:

```
public/
├── sad_hamster.png        # Грустный хомяк на экране предложения
└── hamster_jumping.gif    # Прыгающий хомяк после согласия
```

### ❓ Зачем 36 фото?

1. **18 фото** (1-18) → для игры Memory (дублируются в 36 карточек)
2. **18 фото** (19-36) → для фонового коллажа на экране предложения
3. **ИТОГО: 36 фото** = полностью используются в проекте

Папка `public/game-photos/last/` с файлами 1-10.avif **не используется** в коде.

---

## 🛠️ Технологии и зависимости

### Основные технологии:

- **Next.js 16.1.6** - React фреймворк
- **React 19.2.4** - UI библиотека
- **TypeScript 5** - типизация
- **Tailwind CSS 3.4.1** - стилизация

### Библиотеки для анимаций:

- **Framer Motion 12.2.0** - анимации (переворот карточек, transitions)
- **@fireworks-js/react 2.10.8** - фейерверк

### Dev инструменты:

- **ESLint** - линтер кода
- **PostCSS** - обработка CSS

---

## 🚀 Запуск проекта

### Установка зависимостей:

```bash
pnpm install
# или
npm install
# или
yarn install
```

### Режим разработки:

```bash
pnpm dev
# или
npm run dev
# или
yarn dev
```

Приложение откроется на `http://localhost:3000`

### Продакшен сборка:

```bash
pnpm build
pnpm start
```

---

## 📱 Адаптивность и поддержка устройств

### ✅ Поддерживаемые устройства:

- **Десктоп:** полная поддержка в любой ориентации
- **Планшеты:** работает в ландшафтном режиме
- **Мобильные телефоны:** **ТОЛЬКО** ландшафтный режим (обязательно!)

### 🔒 Ограничения:

- На устройствах с шириной < 1024px в портретном режиме показывается предупреждение
- Карточки адаптируются под размер экрана (используется `vh` для мобильных)

---

## 🎨 Цветовая схема

- **Фон:** черный (`bg-black`)
- **Карточки (рубашка):** серые (`bg-gray-300`)
- **Акцент:** розово-красный градиент (`from-pink-500 to-rose-500`)
- **Текст:** белый с серыми акцентами
- **Ошибка:** красный (`bg-red-500`)

---

## 🐛 Решение проблем

### Проблема: Карточки не переворачиваются

**Решение:** Проверьте, что установлена библиотека `framer-motion`:
```bash
pnpm install framer-motion
```

### Проблема: Фото не загружаются

**Решение:** 
1. Убедитесь, что все 18 фотографий (1-18) находятся в `public/game-photos/`
2. Проверьте расширения файлов (1-10.HEIC, 11-18.avif)
3. Для HEIC может потребоваться конвертация в AVIF/WebP для браузерной поддержки

### Проблема: Фейерверк не показывается

**Решение:** Проверьте установку:
```bash
pnpm install @fireworks-js/react
```

---

## ✨ Особенности реализации

### 💡 Хитрости кода:

1. **Shuffle алгоритм:** Fisher-Yates для перемешивания карточек
2. **3D переворот:** CSS `perspective` + `rotateY` для реалистичного эффекта
3. **Предзагрузка изображений:** Скрытый div с `priority` для быстрой загрузки
4. **Адаптивный размер:** Использование `vh` вместо `px` для мобильных устройств
5. **Убегающая кнопка:** Динамическое изменение `position: absolute` через inline styles

### 🎭 Анимации:

- **Плавные переходы:** 0.5-2 секунды для разных элементов
- **Пульсация ошибки:** `animate` от Framer Motion
- **Exit анимации:** `AnimatePresence` для плавной смены шагов

---

## 📝 Лицензия

Приватный проект для романтического сюрприза 💕

---

## 👨‍💻 Автор

Создано с любовью для особенного человека на День Святого Валентина 2026 💖

---

**Удачи с сюрпризом! 🎉💕**
